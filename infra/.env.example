# Tempnext — Supabase credentials (example only — real values in .env, not committed)
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=eyJ...
# Service role NEVER committed and NEVER used in test scripts.
# It is only used manually by the operator running migrations against production.
